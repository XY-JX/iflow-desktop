[env]
# 禁用 Tauri 配置文件的自动重载
TAURI_DEV_WATCH_IGNORE = "tauri.conf.json, Cargo.lock"
TAURI_SKIP_DEV_WATCH = "true"

# 并行编译优化
[target.'cfg(all())']
rustflags = ["-C", "codegen-units=256"]

# 开发环境使用更少的优化级别
[build]
jobs = 16
